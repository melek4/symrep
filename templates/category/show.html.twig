{% extends 'base.html.twig' %}

{% block title %}Category
{% endblock %}

{% block body %}
	<h1>Category</h1>

	<table class="table">
		<tbody>
			<tr>
				<th>Id</th>
				<td>{{ category.id }}</td>
			</tr>
			<tr>
				<th>Label</th>
				<td>{{ category.label }}</td>
			</tr>
		</tbody>
	</table>

	<a href="{{ path('category_index') }}" class="btn btn-outline-dark">back to list</a>

	<a href="{{ path('category_edit', {'id': category.id}) }}" class="btn btn-dark">edit</a>

	{{ include('category/_delete_form.html.twig') }}
{% endblock %}
